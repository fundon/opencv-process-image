project( gamma_correction )
cmake_minimum_required( VERSION 2.6 )
find_package( OpenCV REQUIRED )
file(GLOB SRC_FILES "src/*.cpp")
add_executable( gamma_correction ${SRC_FILES} )
target_link_libraries( gamma_correction opencv_core opencv_imgproc opencv_highgui)
