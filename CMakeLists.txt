project( process_image )
cmake_minimum_required( VERSION 2.6 )
find_package( OpenCV REQUIRED )
file(GLOB SRC_FILES "src/*.cpp")
add_executable( process_image ${SRC_FILES} )
target_link_libraries( process_image opencv_core opencv_imgproc opencv_highgui opencv_imgcodecs)
